[package]
name = "{{project-name}}-fuzz"
version = "0.0.0"
publish = false
edition = "2021"

[package.metadata]
cargo-fuzz = true

[dependencies]
libfuzzer-sys = "0.4"

[dependencies.{{project-name}}]
path = "../{{project-name}}"

[[bin]]
name = "fuzz_example"
path = "fuzz_targets/fuzz_example.rs"
test = false
doc = false
bench = false

# Declare as standalone workspace to avoid being pulled into parent workspace
[workspace]
